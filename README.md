基于Python3开发的一门语言。它的运行依赖于Python3解释器。  
This is a language developed based on Python3. It requires Python3 interpreter to run.  

要使用这门语言开发，需要安装Python3。  
If you want to program with this language, you need to install Python3.  

提倡Modules主动发生行为，而不是被他人调用。  
This language encourages modules to take the initiative to act instead of being called by other modules.  

详见代码示例。  
You can see the code examples for details.  

运行：  
Run:  

$ cd ./examples  
$ evental run  
